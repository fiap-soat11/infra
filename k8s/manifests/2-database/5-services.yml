apiVersion: v1
kind: Service
metadata:
  name: fiap-db-svc
  namespace: fiap-soat11
spec:
  type: ClusterIP
  selector:
    app: fiap-db-deployment
  ports:
    - port: 3336
      targetPort: 3306
      protocol: TCP
      name: db