apiVersion: v1
kind: Service
metadata:
  name: fiap-svc-pagamento
  namespace: fiap-soat11
spec:
  selector:
    app: fiap-deployment-pagamento
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer